# 🛠️ Sketch to SolidWorks DXF Converter (Streamlit App)

This app lets you upload a sketch (hand-drawn or digital), detects edges using OpenCV, and generates a `.DXF` file that can be imported into SolidWorks for further modeling.

## 🚀 How to Run

```bash
git clone https://github.com/yourusername/image-to-dxf-app.git
cd image-to-dxf-app
pip install -r requirements.txt
streamlit run app.py
